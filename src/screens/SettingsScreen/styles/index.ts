import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  container: {
    textAlign: "center",
    fontSize: 20,
    fontWeight: "bold",
    color: "red",
    marginTop: 20,
  },
});

export default styles;
